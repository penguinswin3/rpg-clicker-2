# TODOs

## Systems

## Bug Fixes

## UI Tweaks 
- Should the buttons inherit the color of the class who owns them? 

## Code 
- All "unlocked" flags should be extracted into a "game state" type object when not tied to a specific service. Defaults can still be in the game config but persistent game state should be elsewhere 